/// How a time series should be rendered in the UI.
///
/// - [line]: Use a line chart (default for overview)
/// - [candles]: Use a candlestick chart (default for detail/deep-dive)
///
enum SeriesViewMode {
  line,
  candles,
}
