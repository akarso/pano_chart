## PR-033 macro events fixes

- when entering macro events page, events seem to be presented a bit randomly. I need them to be scrolled up so, that the closest future event will be one row below the top edge of content area:

-----------
old event
new event (closest one to happen)
new event 2
new event 3
[...]
-----------

- now - when entering from the chart, we should have an info which event on the chart was tapped. If it was a cluster of events, choose first one. Scroll contents of the event page so that selected event be centered vertically

- if centering selected event vertically is not possible (may be just one evnet in the future, so no option to scroll it to center without blank space afterwards), just scroll it 'as much as possible'

- this selected event should be visually highlighted - green border should be fine. This highlight lasts only until event page closed