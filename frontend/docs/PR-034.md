## PR-034 sparkline page changes

- first of all I need spice up loading of the charts visually: try loading them with some delay one after another and showing with another delay. So this would be like: display: none changed to block (and this would be delayed N miliseconds with each sparkline chart) and then opacity from 0 to 1 with delay M miliseconds. Visually I want charts to load one after another, but quickly, and gradually appear one after another, too (opacity part)

- let's try first delay (N) 10ms, and second (M) 20ms

- this needs to be done only in the viewport, so use some intersection observer for this

- this needs to be used whenever there is loading of the charts, be it refresh, load more, etc

- make this helper class or method decoupled as much as it can be. This may be used later on a automatically refreshed charts, so don't rely on hardcoded values like 'first row', calculate viewport visibility.

- also underlying animations may vary, so if you would be able to create highly abstract 'sequential executor of things visual' class or interface, that may be desirable. And then pass things to animate, properties, concrete implementations, keeping common calculations of viewport window, delays

- I would also like, that after refresh of the overview page, last value on the sparkline be compared to the one before refreshing. If it's greater than before - make it visible by placing and animating a point at the end of sparkline chart (newest one). This should look like bright dot with glow of the same color. Think in terms of 'flashing for a second'. Green for positive change, red for negative. This should also fire up with delay of about 10ms between charts, after they are all loaded and 'flash' animation should take about a second.

- put small info at the top of overwiew page 'stale content, pull down to refresh' when some time since last chart loading passes. 1m - after 1m30s, 5m - after 2m30s, 15m - after 5m, 1h - after 10m, 4h and 1d after an hour

- also when dealing with connectiivity problems, make sure info about serving cache content at the top of the page will be there until connectiivity is back and fresh charts are loaded

- make sure not to put messages (pull down to refresh, no connection) one over another, they must not overlap visually