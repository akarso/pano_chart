
- make labels near dropdowns and dropdowns 20% smaller

- make sure nothing from the settings part crosses the padding, I want it to be 'visually elegant'

- add settings and menu overlay. I put menu and settings (gear) svg icons in frontend/assets. Generally, menu icon should go on the top right, settings left of it. Both of them should call an overlay. Settings overlay: all the controls (drpdowns) currently present. Menu overlay: working, but empty. Just put there 'Help' for now, we will fill it up later. Menu and settings icon - when overlay visible - change to [x] (which obviously has to close overlay). When one overlay is opened and the other is called to open, currently opened must close (only one opened at a time)

- add logo - for now just put 'PanoChart' text top left, will put there svg later. Whole top area like 50px more or less must be 'navigation panel' of sorts, wiith branding. Logo, settings, menu. Add bottom border 1px solid grey and make bg of this panel #333. Remember about paddings everywhere, no element must touch top panel border from any side.

- add reloading pull down style. This one should be understandable. When scrolled to the top and trying to scroll even more - call refresh/reload of current query.
