### UI changes

- one letter indicator labels in top right of sparkline: make them: 1 column - keep current size, 2 column: 90% of current size, 3 column: 80% of current size
- ticker/token names in sparkline: 0.9 opacity in 1 column view, 0.8 opacity in 2 column, 0.7 opacity in 3 column
- looks like everyting is priced vs USDT, so drop that part from the token name, I can put explanation in help popup for this. Just str replace USDT -> ''
- menu rollout on top - right now this moves area below when opened. I want this to have greater z-index and come on top of the underlying view. Make its bg opacity 0.9. Animate open and close, but no longer than 200ms.
- second row (and every next) in rollout - divide by dark grey dotted horiz line #666 will be fine, of course add some margin to the line (top and bottom)
- general rule - keep sizing relative. Em, vw, vhw and so on, so this would scale nicely on different screens
- detail screen - swap close and favourite. I want close to be in the same place (roughly) where are other close icons on the screen - top right
- overview screen 3 column - make margin between sparkline items 50% lesser - but only between items, margin to the container border is ok. Also make border radius 50% less in 3 column
- add 3px #444 border to the top of the app