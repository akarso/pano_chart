PR-020

##Frontend adjustments

We need to catch up with backend:

- Group options in two groups, separated by a line:
  - sideways, compression, breakout, trend
  - gainers, losers, volume

    from these - some need to have up and down as secondary sort option, this can be in place of 'sideways algo' currently present. compression, breakout, trend - these need to have up & down switch

- remove 'sideways algo' currently present, I can switch it using env vars for debugging

- right now we have 'gain' mode, there needs to be very similar one, just the other way round, these two modes will be attached to options 'gainers' and 'losers' respectively

- delete sorting by 'combined metascore', this is useless, remove from the dropdown (keep in backend)

- breakout option currently does nothing, we will address it in next PR
