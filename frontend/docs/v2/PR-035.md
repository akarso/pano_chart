## PR-035 social feed fixes

- first of all - adding accounts works fine, from what I can see, but there is no feedback if it works or not, and before initializing you are still left with 'no accounts followed, add one'

- also in this topic - I'm not sure where is account add rollout once accounts are there. It must be called also from icon somewhere on the top

- I think also on this subscription rollout there has to be 'allow notifications' in general, because we do not allow them by default. So this rollout would be called from gear icon on top, or (when nothing added yet) as now - from the empty screen

- We have to make sure notifications work. Do I need to give special rights to the app in the play store, or something?

- In the notification there should be account name, handle, local date, short excerpt

- When adding an account to track, there have to be settings per account (so again - small icon beside +/- probably)
  - omit retweets
  - minimal body lenght
  - keywords to track (max 6, input to enter a keyword and a green button - like now when adding accounts, sort of tag cloud design)

- this implies changes on the backend, too. In addition to what is being done right now, there has to be filtering by content length, keywords and if it is retweet or not. This would be better performed after redis part, I think - it means, do not cache filtered results, too many options. Cache raw ones and filter before returning response

- either these setting have to be stored in sqlite per client (not sure if we can just get phone id and use it) or api endpoint has to have proper params added

- some symbols (namely apostrophe) are mangled in the feed, encoding issue?

- last thing, quite important: put all effectively tracked (after filtering) news on the detail chart, the same way macro events are now placed there. Add icon to disable this, of course. We should keep history of past entries like a week long, otherwise we won't have anything to show on higher timeframes. Use some blue color as main theme for the chart news presentation, to differentiate from macro events

