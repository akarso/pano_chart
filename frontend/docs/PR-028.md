## PR-028 UI fixes

- when I click on the sparkline, I'm being redirected to wrong timeframe. From 1h ->15m, fromm 15m -> 5m and so on, this must be fixed in the first place

- we have to 'prefetch' as many candles in detail view as required to draw the longest ema and rest of the indicators from the beginniing of the chart, but these candles must not be visible on the chart. So if we need 20 candles more, we have to get 110 + 20 (if the base is now 110)

- next we also need to get more candles in general, because now the charts are to narrowly scoped. So I suggest getting 600 + whatever margin is needed (20? 30?)

- there must be also vertical scaling on the chart, solved generally as the horizontal one, price labels and lines move accordingly, event labels stay in place, indicators do not scale, only ema clouds and any indicator in chart area, in general

- this scaling must be set to proper value when coming from sparkline to detali view. I want full chart to be loaded in the same ratio and time span as the sparkline, then user can move around and scale as he wishes. So if now we show last 110 candles of any timeframe, we have to:

    - caclulate timespan from these candles, 
    - set the same timespan on the full chart so that it would fill whole visible width on device, 
    - adjust ratio (height, namely), 
    - scale vertically initially to show proper ratio
    - rest of the chart is being loaded 'in the backrgound', so that user can scroll back a few screens
    - proper time labels are calculated on whole chart area (also invisible one)
    - indicators are calculated on whole chart (also invisible part)
    - remember to fetch a couple of candles more purely for indicators calculation (these candles do not show on the chart)

- the reason for kinda switching to time based view from candle based, is because we might want to offer different levels of detail on sparkline (low/medium/high) all within the same timespan

- when scrolled to lookback limit, add an information with red tiny font alongside left chart border, tilted 90 grades left: 'Hard candle limit reached'