name: pano_chart_frontend
description: Minimal frontend scaffold for Pano Chart (PR-001)
publish_to: 'none'
version: 0.0.0
environment:
  sdk: '>=2.18.0 <3.0.0'
  flutter: '>=3.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
