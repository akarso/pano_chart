## PR-062 social feed and notification fixes

- not sure how is it done now, but we need one source of truth. So no filtering client side - this has to be done server side and only quried from the app

- speaking of which - we need to refresh tweet list when: push arrives and list has focus (would be weird if we had notification but had to wait 30s for list update), on focus, of course on user initiated reload, on settings change (filtering)

- notification has to redirect to social feed when clicked, now it just redirects to app in general

- while we're at it - all other notifications have to redirect correctly too:
  - news notification to news page
  - upcoming event to macro event page
  - any of the three trends to the market pulse page
  - setup of the day to the relevant token detail chart

- in the notifications tab 'daily market summary' has to be further split into 'uptrend', 'downtrend', 'sideways' - each has to be toggable separately, but notification behavior is: check the market index chart for any of enabled metrics, if one or more is over 75% dominance level - choose the strongest one. Say - user toggles 'sideways' and 'uptrend' for notifications. At 7 a.m. none of them is above 75%. This lasts up to 9:10 a.m. (local time) when 'sideways' reaches 77%. Notification pops up, which on tap redirects to market pulse page

- also I am sorry, but these percentages have to be user configurable. So for 'uptrend', 'downtrend', 'sideways' there have to be input fields with default value 75% and label 'min. dominance'. Same goes for setup of the day, only label there would be 'min. quality score'. These have to be saved and used for each client for filtering. Since were dealing with notifications when app is in the backend, we have to store this config server side (and actually any per user notification config)